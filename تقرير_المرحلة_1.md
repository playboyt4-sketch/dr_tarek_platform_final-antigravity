# تقرير المرحلة 1 — الأساس + Student Home + الداشبورد الكامل

**المشروع:** Dr. Tarek Platform
**التاريخ:** 2026-08-17
**النطاق:** برومبت المرحلة 1 (الأجزاء أ + ب + ج) فقط — بدون أي تعديل على Authentication أو Device Binding أو Video Streaming الأساسي بخلاف ما هو مذكور صراحة في البرومبت.

---

## أولًا: نتائج التحقق الفعلي (الأوامر الإلزامية)

| الأمر | النتيجة | ملاحظات |
|---|---|---|
| `flutter analyze` | **ناجح** | `No issues found!` — صفر أخطاء وصفر تحذيرات |
| `flutter test` | **ناجح** | **52 اختبارًا ناجحًا بالكامل** (48 السابقة + 4 اختبارات Student Home الجديدة + اختبار رفض الإيقاف التأديبي في EntitlementResolver + 3 اختبارات كيان الفترة الدراسية) |
| `cd functions && npm run lint` | **ناجح** | 0 أخطاء (12 تحذير `no-explicit-any` قديمة موجودة قبل هذه المرحلة ولم تتغير) |
| `cd functions && npm run build` | **ناجح** | `tsc` يبني بدون أخطاء |
| `cd functions && npm test` | **ناجح** | اختبارات دورة حياة الفترة الدراسية ورفض غير الـ Teacher — `Academic period function tests passed.` |
| `firebase emulators:exec` (security-tests) | **ناجح** | `Firestore and Storage rules tests passed.` — تشمل القواعد الجديدة لـ `academic_periods` و`platform_features` وصلاحيات الأدمن الوظيفية |

---

## ثانيًا: جدول حالة البنود

### جزء أ — إغلاق الفترات الدراسية وتوحيد الـ Entitlement

| البند | الحالة | الدليل |
|---|---|---|
| أ.1 — الحقول التأديبية على الاشتراك (`manually_disabled`, `disabled_by`, `disabled_at`, `disabled_reason`) | **اتنفذ كامل (تحقق فقط — كان موجودًا وسليمًا)** | تحققت من وجودها في `MembershipSubscription` (`lib/features/membership/domain/entities/student_subscription.dart`)، وفي `EntitlementResolver._isEligibleSubscription` (يرفض `manuallyDisabled`)، ودالة `setSubscriptionDisciplinaryStatus` (Teacher-only عبر `requireTeacher` + transaction + `admin_audit_log`)، وربطها في `assertSubjectAccess` في الـ Functions. أضفت فقط اختبارًا مخصصًا يثبت الرفض. |
| أ.2 — مخطط `academic_periods` المعتمد (`label`, `is_core`, `status: active/ended`, `started_at`, `ended_at`, `created_by`, `created_at`) | **اتنفذ كامل** | أُعيد كتابة دوال الفترات في `functions/src/index.ts` بالمخطط المعتمد حرفيًا. `period_type` حقل نصي مفتوح (القيم الحرة للفترات المضافة تُخزَّن كما هي). التوافق مع البيانات القديمة محفوظ عبر `normalizePeriodStatus` و`serializeAcademicPeriod` (الحالات القديمة `STARTED/ENDED/NOT_STARTED` تُقرأ كـ `active/ended`). |
| `setAcademicPeriodStatus(periodId, status)` — Teacher-only + transaction + `started_at`/`ended_at` + audit | **اتنفذ كامل** | الدالة تستخدم `requireTeacher` حصريًا، transaction، تكتب `started_at` عند التفعيل و`ended_at` عند الإنهاء، وتسجل في `admin_audit_log`. |
| `createExceptionalAcademicPeriod(label)` — Teacher-only + `is_core: false` | **اتنفذ كامل** | تنشئ فترة `is_core: false` بحالة `ended` (رمادي) حتى يفعّلها المعلم. يقبل `label` و`periodType` حر اختياري. |
| ربط الاشتراكات بالفترة (`academic_period_id`) + الإنهاء التلقائي `end_date = ended_at` ضمن batch | **اتنفذ كامل** | `activateFreePlan` و`renew` و`enforceOneSubscriptionPerSubject` تختم الاشتراك بـ `academic_period_id` للفترة الأساسية النشطة. عند إنهاء فترة، كل اشتراك `active` مرتبط بها يتحدث إلى `status: expired` و`end_date = ended_at` في batch واحد. مغطى باختبار وظيفي يتحقق من المساواة الحرفية بين `end_date` و`ended_at`. |
| الاستبدال النهائي لـ `system_settings.current_term_end_date` | **اتنفذ كامل** | حُذفت دالة `getCurrentTermEndDate` بالكامل واستُبدلت بـ `getActiveCoreAcademicPeriod` + `resolveSubscriptionPeriodBinding`. لا يوجد أي مرجع متبقٍّ لـ `current_term_end_date` في الكود (تحقق `grep` شامل). **سلوك جديد مقصود:** إنشاء/تجديد الاشتراك يفشل برسالة واضحة إذا لم تكن هناك فترة أساسية نشطة — لأن الفترة هي المصدر الوحيد لحدود الاشتراك الآن. |
| Firestore Rules لـ `academic_periods` | **اتنفذ كامل** | `allow get, list: if signedIn(); allow write: if false;` — مضافة ومختبرة في emulator (قراءة طالب/معلم تنجح، قراءة مجهول وكتابة معلم/أدمن تفشل). |
| حذف/عدم استخدام `canManageAcademicTerms` | **اتنفذ كامل (تحقق)** | الدالة غير موجودة أصلًا في `firestore.rules` — لا يوجد أي تفويض لإدارة الفترات؛ كل الدوال Teacher-only. |
| واجهة الداشبورد للفترات (أساسية أولًا بترتيب ثابت، ثم المضافة، `CupertinoSwitch` واحد، زرار إضافة) | **اتنفذ كامل** | `AcademicPeriodsScreen` أُعيدت كتابتها: الفترات الأساسية الثلاث أولًا بترتيب ثابت (`display_order`)، ثم الاستثنائية، كل فترة معها `CupertinoSwitch` واحد فقط (أخضر = نشطة، رمادي = منتهية)، وزرار "+ إضافة فترة استثنائية" أسفل القائمة، وزرار "تهيئة الفترات الأساسية" يظهر فقط عند غيابها. الواجهة بالعربية بالكامل وتعرض `label`. |

### جزء ب — شاشة Student Home

| البند | الحالة | الدليل |
|---|---|---|
| خلفية ملوّنة كامل الشاشة من design tokens | **اتنفذ كامل** | الخلفية من `AppColors.gradeOne/Two/Three/Four` (نفس ألوان مستند "الون الطلبة في المنصة" المعتمد: `FBBC05/4285F4/34A853/EA4335`) حسب فرقة الطالب الفعلية، مع fallback للأولى. لم يُخترع أي لون جديد. |
| `UserAvatar` بالحلقة والشارة الصحيحتين | **اتنفذ كامل** | يستخدم المكوّن الموجود `UserAvatar.fromAuthValues` بالدور الفعلي، والشارة من مفتاح الخطة الفعلي للطالب (يُجلب من أول اشتراك نشط → `plans.plan_key` → free/pro/max). |
| "Hi, {الاسم الأول}" ديناميكي | **اتنفذ كامل** | الاسم الأول يُستخرج من `user.fullName` الفعلي. مختبَر. |
| قائمة أزرار Pill لمواد الطالب الفعلية | **اتنفذ كامل** | كل زرار pill أبيض بيضاوي بعرض شبه كامل = مادة واحدة من `dashboardSubjectsProvider` (نفس مصدر `subject_access_assignments` → اشتراك → خطة). **غيّرت مصدر البيانات ليستبعد المواد ذات `enabled != true` نهائيًا** (كانت تظهر معطّلة سابقًا) — فقط المفعّلة وغير المحذوفة والظاهرة تظهر. الضغط يفتح `SubjectNavigationScreen` الموجود. |
| حالة الاشتراك مرتبطة بالمادة | **اتنفذ كامل (بافتراض موثّق)** | المرجع البصري المرفق (صفحة 12 من `education - os ui.pdf`) لا يوضح شكل badge الحالة، فطبّقت الحد الأدنى المسموح به في البرومبت: **نص صغير تحت اسم المادة داخل الـ pill** (اشتراك نشط / فترة تجريبية / مجمّد / منتهي مع الفترة / موقوف تأديبي / لا يوجد اشتراك) بلون دلالي من التوكنز الموجودة. أضفت حالة `disciplinaryDisabled` لكيان `DashboardSubject` حتى تظهر حالة الإيقاف التأديبي للطالب. |
| Bottom Navigation بـ 3 عناصر فقط (Home نشط، Chat، Notifications) | **اتنفذ كامل** | `BottomNavigationBar` بثلاثة عناصر حرفيًا. تاب Chat يفتح `ChatScreen` وNotifications يفتح `NotificationsScreen` — كلاهما موجود مسبقًا وأضفت لهما وضع `embedded` حتى يعملا داخل التابات بدون Scaffold مكرر. مختبَر (عدد العناصر = 3). |
| نقطة الدخول المباشرة بعد تسجيل الدخول | **اتنفذ كامل** | `AuthGate` يوجّه الطالب المعتمد إلى `StudentHomeScreen` مباشرة بدل `StudentDashboardScreen`. لا شاشة وسيطة. اختبار `auth_gate_test.dart` حُدّث ويثبت الوصول المباشر. الشاشة القديمة لم تُحذف (لا تزال مستخدمة داخليًا في اختبارات أخرى) لكنها لم تعد في مسار الدخول. |

### جزء ج — الداشبورد كمركز تحكم كامل

| الشاشة | الحالة | الدليل |
|---|---|---|
| إدارة الطلاب (قائمة + اعتماد + تحويل نوع + إيقاف تأديبي) | **اتنفذ كامل** | شاشة `StudentsManagementScreen` جديدة: قائمة كل الطلاب (جدد + حاليين)، شاشة تفاصيل لكل طالب فيها تحويل النوع عبر `convertStudentType`، وتفعيل/تعطيل وصول كل مادة عبر `setSubjectAccess`، والإيقاف التأديبي لكل اشتراك عبر `setSubscriptionDisciplinaryStatus` — كلها دوال موجودة فعليًا لم تُعد كتابتها. الاعتماد الأولي يبقى عبر شاشة "اعتماد الطلاب" الموجودة (`approveStudent`). |
| إدارة الأدمنز (إضافة/حذف + صلاحيات بـ `CupertinoSwitch`) | **اتنفذ كامل** | شاشة `AdminsManagementScreen` جديدة (Teacher-only): إضافة أدمن بـ UID مع اختيار الصلاحيات، حذف أدمن (يسحب الصلاحيات ويعطّل الحساب الإداري)، وتبديل كل صلاحية بـ `CupertinoSwitch`. المفاتيح هي نفس مفاتيح `activeAdminPermission` في `firestore.rules` حرفيًا (`admin_students`, `admin_content`, `admin_chat`, `admin_analytics`, `admin_devices`, `admin_payments_view`, `admin_settings`). أُنشئت 3 دوال جديدة `addAdmin` / `removeAdmin` / `setAdminPermissions` — كلها `requireTeacher` + transaction + audit + تحديث custom claims. |
| إدارة المواد (إضافة/حذف soft delete/تعديل) | **اتنفذ كامل** | لم تكن هناك Cloud Function لإدارة المواد، فأُنشئت `createSubject` / `updateSubject` / `deleteSubject` **بنفس نمط `setSubjectAccess` بالضبط** (صلاحية `requireContentManager` = معلم أو أدمن مفوّض `admin_content`، transaction، `admin_audit_log`). الواجهة `SubjectsManagementScreen` تدعم الإنشاء والتعديل (اسم/وصف/ترتيب/ظهور) والحذف الناعم، وتغييراتها تنعكس فورًا على الطالب لأن Student Home يقرأ `is_visible`/`is_deleted` من Firestore مباشرة. |
| جدول الجودة (`PlanQualityMatrixScreen` + `setPlanQualityFeature`) | **اتنفذ كامل (تحقق)** | الشاشة موجودة ومكتملة ومربوطة فعليًا بـ `setPlanQualityFeature` وتستخدم `CupertinoSwitch` أصلًا. لم تحتج أي تعديل. |
| الفترات الدراسية | **اتنفذ كامل** | انظر جزء أ.2. |
| Feature Matrix عام (`platform_features`) | **اتنفذ كامل** | أنشأت collection `platform_features` بنفس نمط `plan_features` (مفتاح + enabled + label)، ودالة `setPlatformFeature` (Teacher-only + transaction + audit)، وشاشة `PlatformFeaturesScreen` لإضافة ميزة جديدة وتبديلها بـ `CupertinoSwitch`. القواعد: الطلاب يقرأون الميزات المفعّلة فقط، الطاقم يقرأ الكل، والكتابة عبر الدالة فقط — مختبرة في emulator. |
| تعليمة `CupertinoSwitch` الإلزامية | **اتنفذ كامل** | كل قرار ثنائي في كل شاشات الداشبورد (الفترات، صلاحيات الأدمن، وصول المواد، الإيقاف التأديبي، ظهور المواد، ميزات المنصة، جودات الفيديو، واعتماد مواد الطالب الجديد) يستخدم `CupertinoSwitch`. تحقق آلي: لا يوجد أي `Checkbox` أو `Radio` في شاشات الداشبورد، وحوّلت `SwitchListTile` الوحيدة المتبقية (حوار اعتماد الطالب) إلى `CupertinoSwitch`. |

---

## ثالثًا: الاختبارات الجديدة الإلزامية

| الاختبار المطلوب | أين نُفّذ | النتيجة |
|---|---|---|
| دورة حياة الفترة الدراسية وربطها التلقائي بـ `end_date` | `functions/test/academic_periods.test.js` — بدء فترة ثم إنهاؤها مع اشتراك مرتبط، مع التحقق أن `end_date == ended_at` حرفيًا وأن الحالة `expired` | **ناجح** |
| رفض `setAcademicPeriodStatus` / `setSubscriptionDisciplinaryStatus` من غير Teacher | نفس الملف — admin وstudent ومجهول لكل دالة (+ `createExceptionalAcademicPeriod`) | **ناجح** |
| عرض مواد الطالب الصحيحة في Student Home (المفعّلة فقط) | `test/student_home/student_home_test.dart` (4 اختبارات: الترحيب + التابات، pill لكل مادة مع سطر الحالة، الحالة الفارغة، لون الخلفية حسب الفرقة) + تعديل مصدر البيانات نفسه ليستبعد `enabled != true` | **ناجح** |
| صلاحيات الأدمن المتعددة تفعّل/تعطّل الوصول فعليًا | `security-tests/rules.test.js` — أدمن بـ `admin_content` يقرأ المواد ويُرفض من سجلات الطلاب، وأدمن بـ `admin_students` العكس، وأدمن `is_active: false` يُرفض من الاثنين | **ناجح** |

---

## رابعًا: الافتراضات المتخذة (موثّقة حرفيًا)

1. **شكل badge حالة الاشتراك في Student Home:** صورة Figma المرفقة الوحيدة لشاشة الطالب الرئيسية (الصفحة الأخيرة من `education - os ui.pdf`) تُظهر الخلفية الملوّنة والترحيب "Hi, tarek" لكنها لا توضح شكل مؤشر حالة الاشتراك. طبّقت الحد الأدنى المنصوص عليه في البرومبت: نص صغير تحت اسم المادة داخل الـ pill، بدون أي عنصر بصري إضافي.
2. **لون الخلفية:** المرجع يعرض الأصفر. ربطته بتوكنز الفرق الأربع المعتمدة في مستند "الون الطلبة في المنصة" (الأصفر للفرقة الأولى... إلخ) لأن البرومبت ينص على "اللون حسب هوية كل شيء" ويمنع اختراع ألوان. الفرقة غير المعروفة ترجع للأصفر (لون الفرقة الأولى وهو لون المرجع).
3. **حالة الفترة الجديدة:** الفترات (الأساسية عند التهيئة والاستثنائية عند الإنشاء) تبدأ `ended` (رمادي) لأن المخطط المعتمد ثنائي `active/ended` ولا توجد حالة "لم تبدأ" — والمعلم يفعّلها يدويًا بالسويتش.
4. **إنهاء الاشتراكات عند إنهاء الفترة:** يستهدف الاشتراكات `status == active` المرتبطة بالفترة فقط؛ الاشتراكات المجمّدة أو الموقوفة تأديبيًا لا تُلمس (استقلالية الإيقاف التأديبي المنصوص عليها في أ.2).
5. **إضافة أدمن:** تتم بمعرّف مستخدم (UID) لحساب موجود، لأن إنشاء حسابات المصادقة نفسها خارج نطاق هذا البرومبت.
6. **إنشاء الاشتراك بدون فترة نشطة:** يفشل برسالة "No active core academic period" — هذا مقصود لأن الفترة الأساسية النشطة هي المصدر الوحيد لحدود الاشتراك بعد إلغاء `current_term_end_date`.

---

## خامسًا: ملاحظات تشغيلية للنشر

- يوجد سكربت `functions/migrate_academic_periods.js` محدّث بالمخطط الجديد لتهيئة/ترقية الفترات الثلاث الأساسية على قاعدة الإنتاج، ويمكن بدلًا منه استخدام زرار "تهيئة الفترات الأساسية" في الداشبورد (يستدعي `initializeAcademicPeriods` التي تملأ الحقول الناقصة دون لمس حالة الفترة).
- الاشتراكات القديمة التي لا تحمل `academic_period_id` تُختم تلقائيًا بالفترة النشطة عند أول إنشاء/تجديد لاحق عبر `enforceOneSubscriptionPerSubject` و`renew`.
- لم يتم أي تعديل على Authentication أو Device Binding أو Video Streaming الأساسي.
